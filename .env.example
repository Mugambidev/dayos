# ─────────────────────────────────────────────
# LOCAL DEV: SQLite (zero setup)
DATABASE_URL="file:./dev.db"

# PRODUCTION (Vercel): Use Neon PostgreSQL — free at neon.tech
# DATABASE_URL="postgresql://user:pass@host/dbname?sslmode=require"
# ─────────────────────────────────────────────

# Google Gemini API Key — FREE at aistudio.google.com
# 1. Go to https://aistudio.google.com
# 2. Click "Get API key" → Create API key
# 1,500 free requests/day — no credit card needed!
GEMINI_API_KEY="AIza..."

# Optional: Your name for personalized greetings
USER_NAME="Your Name"

# Database provider: "sqlite" for local dev, "postgresql" for production
DB_PROVIDER="sqlite"
